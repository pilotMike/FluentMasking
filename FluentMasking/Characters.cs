namespace FluentMasking
{
    /// <summary>Character filters</summary>
    public enum Characters
    {
        /// <summary>Removes all characters from the string that are not letters.</summary>
        Letters,
        /// <summary>Removes all characters from the string that are not numbers.</summary>
        Numbers
    }
}